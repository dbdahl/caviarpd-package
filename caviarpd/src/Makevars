LIBDIR = rustlib/target/release
STATLIB = $(LIBDIR)/libcaviarpd.a

PKG_LIBS = -L$(LIBDIR) -lcaviarpd

all: clean

$(SHLIB): $(STATLIB)

$(STATLIB):
	"${R_HOME}/bin${R_ARCH_BIN}/Rscript" ../tools/staticlib.R

clean:
	rm -Rf $(SHLIB) $(STATLIB) # rustlib/target
